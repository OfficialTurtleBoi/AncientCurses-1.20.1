# Ancient Curses Artifact Implementation Plan

Source reference: `C:/Users/cmartinez/Downloads/ancient_curses_artifacts.md`

This file tracks the artifact concepts against the current Forge 1.20.1 codebase.

## Current Status

| Artifact | Status | Current Code/Assets | Notes |
| --- | --- | --- | --- |
| First Beacon | Implemented | `FirstBeaconItem`, `ModClientEvents` beacon turn clamp, beacon networking, first-person renderer, item models/textures | Sustained hold-to-fire beam. Damage fires every 5 ticks (4×/sec) at 2.5× chargeProgress per hit — max 10 DPS at full charge. Server-side movement slowdown (Slowness 0–2) scales with beam progress above 35%. Client-side turning clamp via `ClientTickEvent` START/END pattern: 8°/tick at beam threshold tapering to 1.5°/tick at full charge, using `Mth.wrapDegrees` for correct delta handling. Aim slowdown applies only above 35% charge. |
| Golden Feather | Implemented | `GoldenFeatherItem`, feather particles, item model/property, TurtleCore spell JSON | Heat mechanic: holding builds heat (0→1 in ~45 ticks base); Soaring slows buildup (−0.003/level, min rate 0.01). Overheat forces stop and applies 6s cooldown reduced by Soaring (min 2s). Passive decay ~27s from max when not using. Boost velocity scales multiplicatively: `soaringModifier × heatBoost` where `heatBoost = 1.0 + (heat × 0.6)`. Particle count scales with heat (3–9 during use, 2–5 airborne). Seismic enchant + landing heat ≥ 0.6 ignites nearby targets for 4s using AABB lookup. `getUseDuration` returns 72000 (heat/release are the only stops). |
| Ice Spark | Initial mechanics implemented | `IceSparkItem`; `ThrownIceSpark` | Right-click throws the spark without consuming it. On first impact it deploys roughly 3 blocks above the hit point for 10 seconds, pulsing TurtleCore's Chilled mob effect in a 5 block radius once per second. Each pulse increments the target's Chilled amplifier until TurtleCore's effect pipeline converts it into Frozen and handles Frozen shatter damage. Nearly expired Frozen effects are renewed while targets remain in the aura. Right-click while deployed recalls it early. |
| Plague Idol | Initial mechanics implemented | `PlagueIdolItem`; `PlagueIdolEntity`; `PlagueIdolRenderer` | Places a persistent 40 health idol entity using the item model. The item has 160 durability across deployments, and damage taken while deployed is written back to the picked-up or dropped item. Once per second it snapshots existing harmful effects on nearby non-party living entities, then spreads those source effects to other valid targets within 7 blocks at 75% duration. Owner and TurtleCore party members are protected and can shift right-click to pick it up. Hostile mobs affected by the idol aggro onto it, and killing it drops the idol item unless its durability is exhausted. The idol tracks which source mob gave each spread effect so it can refresh longer durations without recursively using applications from any Plague Idol as new spread sources. The visible pulse uses TurtleCore's pulse aura renderer, tinted to the effect color, and queues different effect pulses so they do not overlap. |
| Voodoo Doll | Implemented | `VoodooDollItem`; `VoodooSoulEntity` soul-link helpers; `ModClientEvents` soul tint render hook | Right-click targets a mob or directly interacts to extract a Voodoo Soul. Soul is a same-type clone keeping the body's model and native AI. Boss detection threshold at 100 max HP: bosses get 20% health (cap 50 HP), 0.28 speed cap, 8.0 attack cap; normals get 40% health (cap 80 HP), 0.52 speed cap, 12.0 attack cap. Attribute scaling reads body base values (`getBaseValue()`) — soul gets 70% of body speed and 60% of body attack damage. `NearestAttackableTargetGoal<Player>` added to direct entity path. Armor slots cleared, client tints clones teal. Body repeatedly slowed while soul active. Mirrored damage and death debuffs (Weakness II, Slowness III, 50% armor reduction) preserved. No loot/XP drops. Note: `TargetingUtils.getTarget(Player)` has no range parameter — 25-block spec is currently unenforced; deferred to a future core mod change. |
| Fathomless Cauldron | Initial mechanics implemented | `FathomlessCauldronItem`; `ThrownCauldronPotion`; `LingeringCauldronCloud`; `CauldronTooltip` | Stores up to 4 potion types at 16 uses each, refilling matching potion stacks instead of duplicating slots. Right-click potions, gunpowder, or dragon's breath onto the cauldron to load it; right-click the cauldron over inventory slots to unload, with Ctrl-moving modifier stacks in bulk. Drinking applies all stored potion effects to the user, while gunpowder or dragon's breath switches the item into charged splash or lingering throws that consume one use from every stored potion plus one modifier item. The item stays mostly visual in the tooltip, using a bundle-style tooltip image plus a single dark gray catalyst control hint beneath it. |
| Hollow Lantern | Initial mechanics implemented | `HollowLanternItem`; `cursed_lantern` block/item remains separate | Right-click reveals nearby living entities with Glowing for 5 seconds, limited to 4 solid blocks of penetration, with 30 second vanilla cooldown. Held item pulses when invisible/sneaking entities are nearby. |
| Bone Flute | Initial mechanics implemented | `BoneFluteItem`; `ModEvents` tick/attack hooks | Right-click charms nearby undead for 10 seconds at 50% chance, or 75% for skeletons. Wither skeletons and Wither are immune. Charmed undead retarget nearby hostile monsters and charm breaks when the owner attacks them. Uses 45 second vanilla cooldown. |
| Echo Stone | Initial mechanics implemented | `EchoStoneItem` | Stores a snapshot on item NBT every 10 seconds while carried. Right-click teleports the player back to the saved position/dimension and restores saved health, then applies 25 second vanilla cooldown. Inventory is untouched. |
| Gilded Tome | Initial mechanics implemented | `GildedTomeItem`; `EnrichmentEffect`; `ModEvents` XP hook | Passively banks 30% of positive XP gains on the first carried tome. Sneak right-click applies the Enrichment mob effect for 2 minutes, doubling incoming XP before banking. Right-click releases banked XP without re-banking its own payout. |
| Soul Compass | Initial mechanics implemented | `SoulCompassItem`; `ModEvents` kill hook; `ModItemProperties` angle property | When held in the offhand, kills attune the item to the defeated mob type. The item uses vanilla compass assets for now and its needle points toward the nearest living mob of that type within 128 blocks on the client, spinning when unattuned or no match is nearby. |
| Exodus Totem | Initial mechanics implemented | `ExodusTotemItem`; `ModEvents` lodestone/damage hooks | Sneak right-click a lodestone to bind the totem. Right-click and hold for 4 seconds to teleport the user and nearby TurtleCore party members within 8 blocks to the bound position, including cross-dimensional destinations. Taking damage cancels the channel and successful use applies a 60 second vanilla cooldown. |
| Crystal Ball | Missing | None | Needs custom Crystal Heart resource, damage interception ordering, HUD overlay, empty/breaking heart state, cooldown, model/texture. |
| Bloodprice Sigil | Initial mechanics implemented | `BloodpriceSigilItem`; `ModEvents`; `ModKeyMappings`; `ArtifactAbilityPacketC2S`; Curios belt/charm slot data | Right-clicking the sigil in hand, or pressing the artifact ability key while it is worn in a Curios `belt` or `charm` slot, opens a short interception window that converts final incoming damage into Blood Debt on the item. Kills while debt is stored reduce that debt based on the slain target's max health. When the window ends, the sigil waits briefly, then collects the remaining debt as a delayed nonlethal magic hit and applies a cooldown. |
| Thorn Crown | Initial mechanics implemented | `ThornCrownItem`; `ModEvents`; `ModKeyMappings`; `ArtifactAbilityPacketC2S`; Curios head slot/tag data | Can be worn in the vanilla helmet slot or a Curios `head` slot when Curios is present. While worn, direct melee attackers take thorn damage reflected from incoming hits, and every 7 reflected damage stores 1 Thorn Shot charge up to 7 charges. Pressing the artifact ability key while wearing the crown spends 1 charge to fire 7 no-pickup thorn arrows in sequence, either forward based on the player's look direction or in a crouching circle burst around the player. `ThornCrownItem` currently overrides the shared artifact ability activation path provided by the abstract `ArtifactItem` base. |
| Ruination Brand | Initial mechanics implemented | `RuinationBrandItem`; `RuinationMarkEffect`; `ModEvents`; `ModClientEvents`; Curios charm tag data | While equipped in the offhand or a Curios `charm` slot, your hits apply owner-bound Ruination stacks to targets and refresh their duration. Your own damage against branded targets increases with stacks, and reaching max stacks or killing the target detonates the brand for an area burst that damages nearby enemies and heals you plus nearby party allies. Branded targets show their current stack count as a curved arc of sigils above their head. |

## Implementation Surfaces To Reuse

- Artifact base item / ability dispatch for implemented artifact classes: `src/main/java/net/turtleboi/ancientcurses/item/items/ArtifactItem.java`
- Item registration: `src/main/java/net/turtleboi/ancientcurses/item/ModItems.java`
- Creative tab: `src/main/java/net/turtleboi/ancientcurses/item/ModCreativeModeTabs.java`
- Datagen names/models: `src/main/java/net/turtleboi/ancientcurses/datagen/ModLanguageProvider.java` and `ModItemModelProvider.java`
- Server event hooks: `src/main/java/net/turtleboi/ancientcurses/event/ModEvents.java`
- Client event/render hooks: `src/main/java/net/turtleboi/ancientcurses/event/ModClientEvents.java`
- Networking: `src/main/java/net/turtleboi/ancientcurses/network/ModNetworking.java`
- Artifact ability input path: `src/main/java/net/turtleboi/ancientcurses/client/ModKeyMappings.java` and `src/main/java/net/turtleboi/ancientcurses/network/packets/items/ArtifactAbilityPacketC2S.java`
- Particles: `src/main/java/net/turtleboi/ancientcurses/particle/ModParticleTypes.java`
- Entities: `src/main/java/net/turtleboi/ancientcurses/entity/ModEntities.java`
- Curios integration already exists for amulets and can be extended for head/offhand-style artifacts.

## Suggested Build Order

1. Done: Add lightweight item registrations, generated names, simple models, and creative-tab entries for every missing artifact.
2. Done: Add artifacts to Rite/cursed altar rewards as rare bonus loot.
3. Done: Implement lower-risk self-contained artifacts first: `Hollow Lantern`, `Bone Flute`, `Echo Stone`, `Gilded Tome`, `Soul Compass`.
4. In progress: Implement entity/block artifacts next: `Ice Spark`, `Plague Idol`, `Exodus Totem`.
5. Implement damage-pipeline artifacts carefully: `Crystal Ball`, `Bloodprice Sigil`, `Thorn Crown`, `Ruination Brand`.
6. Leave `Voodoo Doll` for its own pass because clone AI, mirrored damage, boss scaling, and soul rendering can touch many systems.

## Settled Design Decisions

- Artifacts should be obtainable as rare loot from Rites/cursed altars.
- Artifact reward chances currently scale by Rite reward tier: 2% / 5% / 10%, awarding one random artifact on success.
- Artifact cooldowns should use the vanilla item cooldown system.
- Party-aware artifacts should use TurtleCore's party system.
- Golden Feather should keep its existing enchantments while adding heat mechanics later. Enchantments can interact with heat, such as lowering overheat or making Seismic convert heat into a fiery landing shockwave.
- `cursed_lantern` and `Hollow Lantern` are separate items with separate textures.
